# Manual Testing

This directory contains manual test cases that require human verification and cannot be automated through unit or integration tests.

Only include test cases here that need a human in the loop, such as:
- UI visual verification
- User experience validation
- Complex multi-step workflows requiring manual interaction
- Features that depend on FreeCAD Gui or external dependencies

Automated tests should be placed in the `tests/` directory instead.
